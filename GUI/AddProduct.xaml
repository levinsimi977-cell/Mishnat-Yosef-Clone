<Page x:Class="MishnatYosef.GUI.AddProduct"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MishnatYosef.GUI" xmlns:local1="clr-namespace:MishnatYosef.BLL"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddProduct">
    <Page.Resources>
        <ResourceDictionary Source="Dictionary1.xaml"></ResourceDictionary>
    </Page.Resources>
   
    <Grid Background="White">     
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width="5*"/>
    </Grid.ColumnDefinitions>
        <StackPanel x:Name="stp" Grid.Column="1">
            <TextBlock Text="הכנס שם מוצר" Style="{StaticResource txt3}" Margin="12,12,12,12"></TextBlock>
            <TextBox x:Name="nameP" MaxLength="14"  Margin="12,0,12,0">
                <Binding Path="NameProduct">
                    <Binding.ValidationRules>
                        <local1:isHebrewRule2/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox>
            <TextBlock Text="בחר קטגוריה" Style="{StaticResource txt3}"  Margin="12,12,12,12"></TextBlock>
            <ComboBox x:Name="cmbC"  Width="112"  Margin="12,0,12,0" DisplayMemberPath="Name" SelectedItem="{Binding Category}" HorizontalAlignment="Right"></ComboBox>
            <TextBlock Text="בחר מותג" Style="{StaticResource txt3}" Margin="12,12,12,12"></TextBlock>
            <ComboBox   x:Name="cmbM"  Margin="12,0,12,0"  Width="112" DisplayMemberPath="Name" SelectedItem="{Binding ProductTrand}" HorizontalAlignment="Right"></ComboBox>
            <Button Style="{StaticResource b4}" Margin="12,12,12,12" x:Name="addPicture" Grid.Column="2" Height="43" Width="106" Content="הוסף תמונה" Click="addPicture_Click" HorizontalAlignment="Right"></Button>
            <TextBlock Text="הקש מחיר" Margin="12,0,12,0" Style="{StaticResource txt3}"></TextBlock>
            <TextBox  Margin="12,12,12,12" MaxLength="6" x:Name="Pprice">
                <Binding Path="Price">
                    <Binding.ValidationRules>
                        <local1:IsNumber/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox>
            <Button x:Name="lett" Content="לאישור" Margin="12,12,12,12" Style="{StaticResource ResourceKey=b3}" Click="lett_Click" Height="40" Width="118"/>
        </StackPanel>
        <ListView x:Name="lstvP" FlowDirection="RightToLeft"  Style="{StaticResource lv2}" Height="auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="שם מוצר" DisplayMemberBinding="{Binding NameProduct}"></GridViewColumn>
                    <GridViewColumn Header="קטגוריה" DisplayMemberBinding="{Binding Category.Name}"></GridViewColumn>
                    <GridViewColumn Header="מותג" DisplayMemberBinding="{Binding ProductTrand.Name}"></GridViewColumn>
                    <GridViewColumn Header="מחיר" DisplayMemberBinding="{Binding Price}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
